includes:
  - 'layer:basic'
  - 'layer:apt'
options:
  apt:
    packages:
      - 'elasticsearch'
      - '-y --allow-unauthenticated'
repo: "https://github.com/jamesbeedy/juju-layer-elasticsearch-base"
